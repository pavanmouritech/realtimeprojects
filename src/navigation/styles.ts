import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    sideMenuProfileIcon: {
      resizeMode: 'center',
      width: 100,
      height: 100,
      borderRadius: 100 / 2,
      alignSelf: 'center',
    },
    textInput:{
      fontSize: 20,
      textAlign: 'center',
      color: 'green'
    },
    signOut:{
      fontSize: 20,
      textAlign: 'center',
    }
  });

export default styles;